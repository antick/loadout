---
import { APP_NAME } from "@loadout/shared";
import Agents from "../components/Agents.astro";
import Capabilities from "../components/Capabilities.astro";
import Download from "../components/Download.astro";
import Header from "../components/Header.astro";
import Hero from "../components/Hero.astro";
import Safety from "../components/Safety.astro";
import Base from "../layouts/Base.astro";
import { AGENT_COUNT } from "../lib/agents";
import { ALL_RELEASES_URL, GITHUB_URL, PARENT_NAME, PARENT_URL } from "../lib/site";
---

<Base
  title={`${APP_NAME}: one skill library for every coding agent`}
  description={`A desktop app that keeps your AI agent skills in one library and links them into Claude Code, Codex, Cursor and ${AGENT_COUNT - 3} other agents.`}
>
  <Header />
  <main>
    <Hero />
    <Capabilities />
    <Agents />
    <Safety />
    <Download />
  </main>
  <footer class="wrap">
    <p>
      {APP_NAME} is a <a href={PARENT_URL}>{PARENT_NAME}</a> project. It is open source under the GPL-3.0:{" "}
      <a href={GITHUB_URL}>source on GitHub</a>, <a href={ALL_RELEASES_URL}>releases</a>.
    </p>
  </footer>
</Base>

<style>
  footer {
    padding-block: 2rem 3rem;
    border-top: 1px solid var(--rule);
    color: var(--slate);
    font-size: 0.9375rem;
  }
</style>
