{
  "name": "@loadout/cli",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "bin": {
    "loadout": "./dist/loadout.mjs"
  },
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:standalone": "node scripts/standalone.mjs",
    "start": "node build.mjs && node dist/loadout.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@loadout/core": "workspace:*",
    "@loadout/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.6",
    "esbuild": "0.28.2",
    "vitest": "5.0.1"
  }
}
